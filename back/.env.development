// back-end (server) 설정
PORT=3085

// front-end (client) 설정
CLIENT_HOST=localhost
CLIENT_PORT=3000

// DB 설정
DB_USERNAME=root
DB_PASSWORD=eum5890
DB_DATABASE=dain_gj
DB_HOST=localhost
DB_PORT=3307

// socket 통신 설정
EXTERNAL_HOST=localhost
EXTERNAL_PORT=8080